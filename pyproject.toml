[project]
name = "bundling-capacity"
version = "0.1.0"
description = "Ordered MAP questionnaire bundle capacity experiment"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false
